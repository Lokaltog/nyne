nyne VFS is active. Use `@/` paths as described in `<nyne-vfs>`. Read with the Read tool, Edit with the Edit tool (`old_string`/`new_string` on symbol bodies), Bash for structural ops and full rewrites.

<project-context>

## Environment

- **Date:** {{ current_date }}
- **Working directory:** {{ worktree_path }}
- **Git branch:** {{ branch }}
- **Project root:** {{ project_root }}
- **Platform:** {{ platform }}
- **Shell:** {{ shell }}

</project-context>
