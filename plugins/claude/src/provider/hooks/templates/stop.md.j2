{% import "macros/hints" as hints %}
{{ hints.header_review("finalization checklist before stopping.") }}

Modified files:
{% for f in changed_files %}
  {{ f }}
{% endfor %}

Checklist:
  [ ] No deviations from the task plan
  [ ] No duplicated logic or repeated similar code blocks
  [ ] No multi-site modifications that could be a single abstraction
  [ ] New constants or functions do not duplicate existing ones
  [ ] New code is covered by tests
  [ ] CLAUDE.md files reflect any new features or changes

If any item is unchecked, fix it before stopping. Report any deviations from the task plan to the user.
