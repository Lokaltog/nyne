{% if is_vfs_symbol_edit and is_enoent %}
The edit succeeded — the symbol was renamed by the content change, so the old path no longer exists. Re-read `{{ rel }}@/symbols/OVERVIEW.md` to discover the new symbol name.
{% endif %}
