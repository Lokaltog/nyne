{% import "macros/hints" as hints %}
{% import "macros/paths" as paths %}
{{ hints.header_hint("instead of " ~ (tool_name | lower) ~ "ing the raw file " ~ rel ~ ", use the symbol-targeted approach.") }}

{{ hints.edit_via_sym(rel, sym, ext) }}
{% if tool_name == "Edit" and sym %}

{#{{ hints.batch_edit_hint(rel) }}#}
{% endif %}
{% if config.include_symbol_table %}

{{ paths.sym_overview(rel) }}:
{% include "syntax/symbol_table" %}
{% endif %}
