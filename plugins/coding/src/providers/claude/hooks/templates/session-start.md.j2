nyne VFS is active. Use `@/` paths as described in `<nyne-vfs>`. Read with the Read tool, write with Bash (`cat <<'EOF' >`, `echo >`, `sed -i`). Never use the Edit tool on `@/` paths.

<project-context>

## Environment

- **Date:** {{ current_date }}
- **Working directory:** {{ worktree_path }}
- **Git branch:** {{ branch }}
- **Project root:** {{ project_root }}
- **Platform:** {{ platform }}
- **Shell:** {{ shell }}

</project-context>
